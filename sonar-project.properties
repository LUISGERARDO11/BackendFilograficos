# Clave del proyecto (ya está correcta)
sonar.projectKey=LUISGERARDO11_BackendFilograficos

# Organización en SonarCloud (ya está correcta)
sonar.organization=luisgerardo11

# URL de SonarCloud (ya está correcta)
sonar.host.url=https://sonarcloud.io

# Token de autenticación (parece correcto, pero asegúrate de que sea válido)
sonar.token=993c7f460af6aa5b703282d5bc6e4a3031ae3d3b

# Directorios de código fuente (incluye src/ y los archivos en la raíz como app.js e index.js)
sonar.sources=src,index.js

# Excluir archivos no deseados (ya está bien, pero ajustemos para JavaScript puro)
sonar.exclusions=**/node_modules/**, **/*.spec.js

# Lenguajes soportados (solo JavaScript para Node.js/Express, eliminamos TypeScript)
sonar.language=js

# No necesitamos tsconfig.json porque no usamos TypeScript en el backend
# Por lo tanto, eliminamos esta línea:
# sonar.typescript.tsconfigPath=tsconfig.json